# Audit & Pembersihan Dead Code — 2026-09-15

Lingkup: seluruh codebase (app + lib + components + cli + scripts). Metodologi: reachability graph + knip@5.79.0 + verifikasi manual per-item ("prove before delete"). Semua penghapusan bersifat **Confirmed Unused**; item Potentially Unused dipertahankan.

## 1. File dihapus

| File | Alasan |
|---|---|
| `components/ui/select.tsx` | 0 import; unreachable di graph; tidak ada konsumen |

## 2. Komponen UI dead (dihapus seluruhnya)

`dialog.tsx`: `DialogTrigger`, `DialogClose`
`dropdown-menu.tsx`: `DropdownMenuPortal`, `DropdownMenuGroup`, `DropdownMenuCheckboxItem`, `DropdownMenuRadioGroup`, `DropdownMenuRadioItem`, `DropdownMenuShortcut`, `DropdownMenuSub`, `DropdownMenuSubTrigger`, `DropdownMenuSubContent`
`card.tsx`: `CardAction`

(12 komponen; seluruh konsumen app hanya memakai komponen yang masih diekspor.)

## 3. Export dilepas (tetap dipakai internal)

- `badge.tsx`: `badgeVariants` (dipakai `Badge`)
- `button.tsx`: `buttonVariants` (dipakai `Button`)
- `dialog.tsx`: `DialogPortal`, `DialogOverlay` (dipakai `DialogContent`)
- `lib/ai/providers/gemini.ts`: `DEPRECATED_GEMINI_MODELS`, `normalizeGeminiModel` (dipakai constructor)
- `lib/prompts/prd-generation.ts`: 8 schema + 8 type (dipakai `prdSchema`)
- `lib/prompts/feature-breakdown.ts`: `featureItemSchema`, `featureBreakdownOutputSchema`, `FEATURE_BREAKDOWN_SYSTEM_PROMPT`, `parseFeatureBreakdownOutput`; type `FeatureItem` dihapus (tidak dirujuk siapa pun)
- `lib/prompts/task-breakdown.ts`: `taskLayerEnumValues`, `taskItemSchema`, `taskBreakdownOutputSchema`, `TASK_BREAKDOWN_SYSTEM_PROMPT`, `parseTaskBreakdownOutput`, `validateTaskDependencies`; type `TaskItem`, `DependencyValidationResult`

## 4. Symbol dead (dihapus)

- `lib/prompts/prd-generation.ts`: `PRD_CHAT_AND_REVISION_SYSTEM_PROMPT`, `buildPrdChatOrRevisionPrompt` (0 importer; `messages/route.ts` tidak menggunakannya; hanya disebut di dokumentasi)
- `lib/auth.ts`: `signOut` (hanya `signIn` dipakai halaman login)
- `lib/utils.ts`: baris `export { cn } from "cn"` (0 importer `@/lib/utils`; komponen UI import package `cn` langsung)

## 5. Dependensi dihapus

- `@tailwindcss/typography` — terverifikasi empiris tidak aktif: tidak ada `.prose` di CSS hasil build, tidak ada `@plugin`, tidak ada `tailwind.config`. Class `prose` di `components/plan-builder/prd-preview.tsx` sudah inert sejak sebelum audit.

## 6. Konsolidasi duplikasi

- **Helper `resolveUserId(req)`** baru di `lib/auth.ts` menggantikan pola 5-baris `auth()` → fallback `validateBearerToken()` yang terulang di 12 route (16 blok):
  - `app/api/tasks/[taskId]/status`, `app/api/features/[featureId]`, `app/api/plans/[planId]/messages`, `app/api/plans/[planId]/features/generate`, `app/api/cli/summarize`, `app/api/plans/[planId]/features`, `app/api/plans/[planId]/tasks/next`, `app/api/settings/tokens`, `app/api/features/[featureId]/tasks/generate`, `app/api/features/[featureId]/tasks`, `app/api/plans/[planId]`, `app/api/settings/tokens/[tokenId]`
- Route pola "session-only" (`projects`, `plans`, `export`, `generate`, `clarify`, `revise`, `versions`, `versions/restore`) dibiarkan — pola tunggal seragam, bukan duplikasi.
- `lib/api-auth.ts`: helper duplikat `getAuthenticatedUserId` (0 pemakai, sempat ditambahkan sebagai alternatif) dihapus; `resolveUserId` menjadi satu-satunya helper autentikasi.

## 7. Potentially Unused (DIPERTAHANKAN)

- `scripts/test-e2e-phase2.ts` — entry test manual, tanpa npm script, tapi impor API lintas service.

## 8. False positive (DIPERTAHANKAN)

- Semua file `cli/src/*` (bin entry `cli/src/index.ts`)
- `app/favicon.ico` (konvensi Next)
- Config tooling: `next.config.ts`, `eslint.config.mjs`, `drizzle.config.ts`, `postcss.config.mjs`
- Deps: `eslint-config-next` (dipakai `eslint.config.mjs`), `postcss` (transitif), `shadcn` (dipakai `app/globals.css` juga)

## 9. Verifikasi

- `npx tsc --noEmit` — bersih
- `npm run lint` — lihat hasil
- `npm run build` — lihat hasil
- `cli`: `npm run build` — lihat hasil
- Runtime spot-check MCP + API — lihat hasil